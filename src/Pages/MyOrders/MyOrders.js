import React from "react"

const MyOrders = () => {
  return (
    // only their order
    // Cancel order(Not paid)
    // Confirmation massage for cancel order(Modal)

    // pay button redirects to payment page
    // Credit card
    // transiction id if the order is completed

    // transiction id will be shown in my Order page
    <div>
      <h2>My Orders</h2>
    </div>
  )
}

export default MyOrders
