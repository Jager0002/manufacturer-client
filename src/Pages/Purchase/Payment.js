import React from "react"

const Payment = () => {
  // const { id } = useParams()
  // const { data: order, isLoading } = useQuery(["rabbit2", id], () =>
  //   axiosBaseUrl(`/order/${id}`).then((res) => res.data)
  // )
  // if (isLoading) return
  return (
    <div>
      <h2>order.addquantity</h2>
      <h2>order.name</h2>
    </div>
  )
}

export default Payment
